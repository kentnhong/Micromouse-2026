// This will enable the encoder test for the F411 board.
// init stuff and also the modes similar to the pwm test, but instead of fetching the pwm values,
// it will fetch the encoder ticks and print them to the console.